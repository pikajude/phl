!!! 5
%html
  %head
    %title PHL
    = stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags

  %body
    - if current_player
      Welcome back, #{current_player.username}!
    - else
      You are nobody.

    .notice= notice
    .alert= alert

    = yield
