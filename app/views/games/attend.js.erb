<% from, to = *(@attending ? ["red", "green"] : ["green", "red"]) %>
$("#game-attendance-<%= @game %> .button")
  .removeClass("<%= from %>")
  .addClass("<%= to %>")
  .html("<%= "not " unless @attending %>coming");
